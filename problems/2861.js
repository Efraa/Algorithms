// https://www.urionlinejudge.com.br/judge/en/problems/view/2861

const input = require('fs').readFileSync('/dev/stdin', 'utf8').split('\n');
const numberOfQuestions = parseInt(input[0]);

for (let question = 1; question <= numberOfQuestions; question += 1) {
  console.log('gzuz');
}
